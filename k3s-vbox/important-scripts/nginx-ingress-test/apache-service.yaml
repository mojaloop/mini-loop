apiVersion: v1
kind: Service
metadata:
  name: apache-service
spec:
#  type: NodePort
  selector:
    app: apache-app
  ports:
    - protocol: TCP
      port: 8181
#      targetPort: 80
#      nodePort: 30036

